## Torque-Modeling 电机力矩模型
- 直接给出推算公式：
  $$ T_o = \frac{m\pi r^2(s_r - s_f)}{120T_s} + \frac{mgrsin\theta}{4} + T_c$$
  $$m为车重，r为轮半径，s_r和s_f为目标转速和反馈转速，T_s为时间常数，\theta为坡度，T_c为摩擦反馈力矩$$
  依靠这个公式，可以通过当前电机转速和目标转速的差值(单位rpm)计算出电机需要的扭矩，然后通过
  电机转矩常数(详细看用户手册，3508为0.3N·m/A)将扭矩转换为电流，再通过对应系数将电流转换
  为报文数据形式发送

